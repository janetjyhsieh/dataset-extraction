[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"
[project]
name = "dataset_extraction"
version = "0.0.0"
dependencies = [
    "openreview-py",
    "anthropic",
    "openai",
    "pydantic",
    "tinydb",
    "requests",
    "beautifulsoup4",
]